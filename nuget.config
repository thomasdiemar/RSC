<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
    <add key="SpaceEngineers" value="https://diemar.pkgs.visualstudio.com/06ec9d7b-fe8e-4f72-a28e-3edb09ab1b81/_packaging/SpaceEngineers/nuget/v3/index.json" />
  </packageSources>

  <config>
    <add key="enablePackageSourceMapping" value="true" />
  </config>

  <packageSourceCredentials>
    <SpaceEngineers>
      <add key="Username" value="diemar" />
      <add key="ClearTextPassword" value="YOUR_PAT_WITH_READ_WRITE_SCOPE" />
    </SpaceEngineers>
  </packageSourceCredentials>

  <!-- 
    Azure DevOps feed authentication
    
    Update the ClearTextPassword value above with your Personal Access Token (PAT) from Azure DevOps.
    The PAT MUST have "Packaging (Read & Write)" scope to:
    1. Restore packages
    2. Access vulnerability data (removes NU1900 warning)
    3. Publish packages (for NuGet publishing workflow)
    
    Steps:
    1. Create new PAT in Azure DevOps with "Read & Write" scope
    2. Replace YOUR_PAT_WITH_READ_WRITE_SCOPE with the actual token
    3. Run: dotnet restore
    
    For CI/CD: Do not commit actual PAT to git repository.
    Instead, use GitHub Secrets for the workflow (AZURE_DEVOPS_NUGET_TOKEN).
    See .github/workflows/publish-nuget.yml for workflow configuration.
    
    Current Status:
    - NU1900 warning persists with Read-only PAT (expected)
    - All functionality works (packages restore, tests pass, builds succeed)
    - Warning will be removed once PAT is upgraded to Read & Write scope
  -->
</configuration>
