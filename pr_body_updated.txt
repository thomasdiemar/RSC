NuGet Configuration and Build Automation Setup

Comprehensive setup for Azure DevOps NuGet feed authentication and local/CI-CD development workflows.

## Changes Included

### nuget.config - Package Source Configuration
- Nuget.org and SpaceEngineers Azure DevOps feed sources
- SpaceEngineers feed authentication via environment variable
- Package source mapping for optimal dependency resolution
- Full setup documentation for local and CI/CD environments

### .env File - Local Development Secrets
- AZURE_DEVOPS_NUGET_TOKEN for secure credential storage
- Added to .gitignore (never committed)
- Loaded automatically by build/test scripts
- Safe credential management pattern

### build.ps1 - Full Build Workflow
Usage: .\build.ps1 or .\build.ps1 -Configuration Debug
- Auto-loads .env file
- Restores dependencies
- Builds solution (configurable)
- Runs all 84 tests
- Error handling & colored output

### test.ps1 - Test-Focused Workflow
Usage: .\test.ps1 or .\test.ps1 -Configuration Release -Verbose
- Auto-loads .env file
- Builds in Debug/Release (configurable)
- Test suite execution with diagnostics
- Verbose option for detailed output

### RCS.Test.csproj - NU1900 Warning Suppression
- Added NoWarn=NU1900 to eliminate build warnings
- Standard approach for read-only PATs with private feeds
- Does not affect functionality or build success

### AGENTS.md - Updated Documentation
- Quick start guide for developers
- Script usage and configuration options
- Environment setup instructions
- Manual command reference

## Setup Instructions

For Local Development:
1. Create/update .env with your PAT: AZURE_DEVOPS_NUGET_TOKEN=your_pat_here
2. Run: .\build.ps1 (full workflow) or .\test.ps1 (tests only)

For CI/CD (GitHub Actions):
1. Configure secret in GitHub: Settings > Secrets > AZURE_DEVOPS_NUGET_TOKEN
2. Workflows use the secret automatically
3. No PAT needed in config files

## Results
✓ All 84 MSTests pass
✓ 0 build warnings (NU1900 suppressed)
✓ Release and Debug configurations both succeed
✓ No hardcoded secrets in repository
✓ Environment variable references only
✓ .env file excluded from git
✓ Support for both local and CI/CD environments

Files Changed:
- Created: nuget.config
- Created: build.ps1
- Created: test.ps1
- Created: .env (in .gitignore)
- Updated: .gitignore
- Updated: RCS.Test/RCS.Test.csproj (NU1900 suppression)
- Updated: AGENTS.md

Closes #8
